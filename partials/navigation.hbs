<nav class="navbar navbar-expand-lg navbar-light justify-content-between" id="navbar-tooldata" role="navigation">
  <a href="{{@blog.url}}" class="navbar-brand">
    <img src="{{@blog.logo}}" id="tooldata-home" />
  </a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav"
    aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
    <ul class="navbar-nav">
      {{#foreach navigation}}

      <li class="nav-item {{#if current}}active{{/if}}">
        <a href="{{url absolute="true"}}" class="nav-link ">
          {{{label}}}
        </a>
      </li>

      {{/foreach}}
    </ul>
  </div>
</nav>